services:
  - type: web
    name: dealer-portal
    env: static
    buildCommand: pnpm install --no-frozen-lockfile && pnpm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 18
